# =================================================================
# ITAÚ API PIX v2 - CREDENCIAIS
# =================================================================
# Client ID e Secret para autenticação OAuth2
ITAU_CLIENT_ID=seu_client_id_aqui
ITAU_CLIENT_SECRET=seu_client_secret_aqui

# API Key (x-itau-apikey) - UUID fornecido pelo Itaú
ITAU_API_KEY=seu_api_key_uuid_aqui

# Chave PIX (CPF, CNPJ, Email, Telefone ou Chave Aleatória)
ITAU_CHAVE_PIX=sua_chave_pix_aqui

# URLs da API
# Produção: sts.itau.com.br | Sandbox: oauthd.itau
ITAU_AUTH_URL=https://sts.itau.com.br/api/oauth/token
ITAU_API_BASE_URL=https://secure.api.itau/pix_recebimentos_conciliacoes_v2_ext/v2

# Certificados mTLS (obrigatório em produção)
# Caminho para o certificado .crt gerado e assinado pelo Itaú
ITAU_CERT_PATH=./certificados-itau/meu_certificado.crt
# Caminho para a chave privada .key
ITAU_KEY_PATH=./certificados-itau/minha_chave_privada.key

# =================================================================
# POSTGRESQL - CONFIGURAÇÃO LOCAL
# =================================================================
# Para desenvolvimento local com PostgreSQL local:
# Exemplo: postgresql://usuario:senha@localhost:5432/financiamento_solsticio
POSTGRES_URL=postgresql://postgres:postgres@localhost:5432/financiamento_solsticio

# Para usar Vercel Postgres (quando publicar na Vercel):
# Descomente e configure as variáveis abaixo:
# POSTGRES_URL=postgresql://user:password@host:port/database
# POSTGRES_PRISMA_URL=postgresql://user:password@host:port/database?pgbouncer=true
# POSTGRES_URL_NON_POOLING=postgresql://user:password@host:port/database

# =================================================================
# SERVIDOR
# =================================================================
PORT=3000
NODE_ENV=development

# =================================================================
# SEGURANÇA
# =================================================================
# Webhook Secret (OBRIGATÓRIO EM PRODUÇÃO)
# Gere um secret forte: openssl rand -hex 32
WEBHOOK_SECRET=desenvolvimento_local_secret_alterar_em_producao

# Validação mTLS - Certificado do Cliente (Itaú)
# Se true, exige que o Itaú envie certificado de cliente (mTLS)
# Se false ou não definido, valida se presente mas não exige
# Configure conforme a documentação do Itaú para webhooks
# ITAU_REQUIRE_CLIENT_CERT=false

# CORS - Origens permitidas (separadas por vírgula)
# Em desenvolvimento local, deixe vazio ou use * (já configurado no código)
# Em produção, defina: https://seu-dominio.com,https://www.seu-dominio.com
ALLOWED_ORIGINS=

# =================================================================
# NOTAS
# =================================================================
# 1. Em desenvolvimento local, o WEBHOOK_SECRET pode ser qualquer valor
# 2. Em produção, gere um secret forte e seguro
# 3. O ALLOWED_ORIGINS só é necessário em produção
# 4. Para PostgreSQL local, instale e configure PostgreSQL antes de iniciar
